<%= form_for @dog do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :breed_id %><br />
    <%= f.collection_select :breed_id, Breed.all, :id, :name %>
  </p>
  <p><%= f.submit %></p>
<% end %>
